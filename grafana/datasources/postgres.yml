apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    url: postgres:5432
    database: freight
    # Local development credentials — see .env.example for configuration
    user: airflow
    secureJsonData:
      password: airflow
    jsonData:
      sslmode: disable
      schema: gold
      postgresVersion: 1600
      timescaledb: false